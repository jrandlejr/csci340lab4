# CSCI340 Lab 4 - Anime Quote Generator

**Approach**:  
Started with Animechan API for random anime quotes. Tried adding Openwhyd for background music but ran into CORS blocks and no suitable no-key API for playable audio (per professor's no-CORS/no-key rule). Pivoted to COLOURlovers API for random color palettes — quote loads → page colors change to match "vibe".

**Changes**
I then realized that the second API I tried to use had a certain limit that generates anime qoutes. So, I decided to further research another API that works perfectly, and generates Anime quotes an infinite number of times. 

